{
  "title": "Place",
  "description": "The parent for any location data",
  "type": "object",
  "properties": {
    "allOf": [
       { "$ref": "./nugget.json" }
    ],
    "type": {
      "type": "string",
      "description": "Derived from the `Thing` object titles defined at schema.org.",
      "enum": ["g","CreativeWork","Event","Organization","Person","Place","Product"]
    }
  },
  "required": ["type"]
}