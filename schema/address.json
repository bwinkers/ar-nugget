{
    "title": "Address",
    "type": "object",
    "properties": {
        "type": {
            "type": "string",
            "enum": ["street", "geo", "multiLine"]
        }
    },
    "required": ["type"]
}