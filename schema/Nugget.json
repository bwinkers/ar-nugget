{
    "title": "Nugget",
    "description": "Core Nugget object, all other objects are extensions of it.",
    "type": "object",
    "properties": {
        "name": {
            "title": "Name",
            "type": "string",
            "description": "A name used to refer to the thing or nugget."
        },
        "coreType": {
            "title": "Core Type",
            "type": "string",
            "description": "Derived from the `Thing` object titles defined at schema.org.",
            "enum": [
                "Action",
                "CreativeWork",
                "Event",
                "Organization",
                "Person",
                "Place",
                "Product"
            ]
        },
        "description": {
            "title": "Description",
            "type": "string",
            "description": "A description of the item or nugget."
        },
        "identifier": {
            "title": "UID",
            "type": "string",
            "description": "The identifier property represents any kind of identifier for any kind of Thing, such as ISBNs, GTIN codes, UUIDs etc."
        }
    },
    "required": [
        "coreType"
    ]
}