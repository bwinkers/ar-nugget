{
    "title": "Nugget",
    "description": "Base object type for everything else",
    "required": [
        "coreType"
    ],
    "properties": {
        "coreType": {
            "description": "The core ActiveRules type",
            "type": "string",
            "enum": [
                "Person",
                "Product",
                "Event",
                "PublishedWork"
            ]
        }
    },
    "extends": "Thing",
    "type": "object"
}