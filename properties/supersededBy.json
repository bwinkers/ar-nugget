{
    "description": "Relates a term (i.e. a property, class or enumeration) to one that supersedes it.",
    "oneOf": [
        {
            "$ref": "#/components/schema/Class"
        },
        {
            "$ref": "#/components/schema/Enumeration"
        },
        {
            "$ref": "#/components/schema/Property"
        }
    ]
}